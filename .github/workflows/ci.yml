name: CI

on:
  push:
  pull_request:
  # Dependencies are declared without upper bounds (see pyproject.toml), so a
  # new upstream release can break the build without any commit here. uv sync
  # resolves fresh on every run, so this weekly job is what surfaces that --
  # without it, "fix it when it breaks" means waiting for a user to report it.
  schedule:
    - cron: "0 18 * * 0"  # Sundays 18:00 UTC (Monday morning AWST)
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # full history so setuptools-scm can read tags

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - uses: astral-sh/setup-uv@v8.0.0
        with:
          cache-suffix: ${{ matrix.python-version }}

      - run: uv sync --all-extras --dev

      - name: Lint
        run: uv run ruff check .

      - name: Type check
        run: uv run mypy s2mosaic/

      - name: Test
        run: uv run pytest tests/ -x -q

      - name: Notebook smoke test
        if: matrix.python-version == '3.11'
        run: uv run pytest --nbmake examples/*.ipynb -q
